{
  "name": "pocket-mint-phase-0",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "check": "node --check public/app.js && node --check public/progress.js && node --check public/identify.js && node validate.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
