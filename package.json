{
  "name": "pocket-mint-phase-0",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "check": "node --check public/app.js && node --check public/progress.js && node validate.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
